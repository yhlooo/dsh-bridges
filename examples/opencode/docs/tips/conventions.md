# Documentation conventions

Injected by the opencode bridge via the `docs/tips/*.md` instructions glob.

- One heading per topic; keep sections short.
- Code blocks carry a language tag.
- Use tables for option reference, lists for prose.

---
name: legacy-helper
description: Legacy helper kept only to demo the [[skills.config]] disable feature.
---

# Legacy Helper

This skill is intentionally disabled by `.codex/config.toml`
(`[[skills.config]]` with `enabled = false`), so the codex bridge skips it
during discovery. It exists purely to show that mechanism working.
